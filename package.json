{
  "name": "labflow-site",
  "version": "1.0.0",
  "private": true,
  "description": "Site vitrine LabFlow — statique (nginx). Outils de capture dans tools/.",
  "scripts": {
    "captures": "node tools/capture.js"
  },
  "devDependencies": {
    "puppeteer-core": "^23.0.0",
    "sharp": "^0.35.3"
  },
  "dependencies": {
    "puppeteer": "^25.3.0"
  }
}
